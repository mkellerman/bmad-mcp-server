# Logging and Observability

- Structured logs with correlation IDs per request
- Log levels: debug/info/warn/error with guidance
- Trace boundaries around tool and resource handlers
- Minimal PII with redaction filters
- Simple dashboards for error rate and latency
